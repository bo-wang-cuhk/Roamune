# Future GitHub Sync Layer

This directory intentionally contains no sync implementation in the local-first MVP.

Its future boundary may own pull, push, merge, conflict handling, encryption, decryption, and remote versioning. UI pages must continue to use repositories rather than depending on a future adapter directly.

